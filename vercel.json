{
    "builds": [{
        "src": "studygate/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "studygate/wsgi.py"
        }
    ]
}